// Global feature flags.
//
// CART_ENABLED toggles the entire shopping-cart / checkout experience across the
// site (Add-to-Cart buttons, the cart icon + badge in the navbar, the /cart and
// /order-success pages). It is currently turned OFF.
//
// To re-enable the cart later, simply set this to `true` — no other code changes
// are required.
export const CART_ENABLED = false;
